// Like in C#, a bit different than Java's
export enum Weekday {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
}

export enum Color {
    Yellow = 121,
    Red = 140,
    Green = 156,
    Blue = 155,
    Black = 255
}